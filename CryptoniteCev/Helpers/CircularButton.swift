//
//  CircularButton.swift
//  CryptoniteCev
//
//  Created by alumnos on 21/01/2021.
//  Copyright © 2021 user177257. All rights reserved.
//

import UIKit

/*
class CustomCell: UICollectionViewCell {
    
    struct CustomData {
    var title: String
    var url: String
    var backgroundImage: UIImage
    }
    
    var data: CustomData? {
        didSet {
            guard let data = data else { return }
            bg.image = #imageLiteral(resourceName: "stockProfilePic")
            
        }
    }
    
    fileprivate let bg: UIImageView = {
       let iv = UIImageView()
        iv.translatesAutoresizingMaskIntoConstraints = false
        iv.contentMode = .scaleAspectFill
        iv.clipsToBounds = true
                iv.layer.cornerRadius = 12
        return iv
    }()
    
    override init(frame: CGRect) {
        super.init(frame: .zero)
        


        
        contentView.addSubview(bg)

        bg.topAnchor.constraint(equalTo: contentView.topAnchor).isActive = true
        bg.leftAnchor.constraint(equalTo: contentView.leftAnchor).isActive = true
        bg.rightAnchor.constraint(equalTo: contentView.rightAnchor).isActive = true
        bg.bottomAnchor.constraint(equalTo: contentView.bottomAnchor).isActive = true

    }
    
}*/

    /*
class CircularButton: UIButton {
    
    var button : UIButton
    
    public func setupCircularButton(button : UIButton){
        
        button.layer.cornerRadius = 0.5 * button.bounds.size.width
        button.clipsToBounds = true
        button.setImage(UIImage(named:"stockProfilePic.png"), for: .normal)
        
    }
    
    override func setNeedsLayout() {
        super.setNeedsLayout()
        setupCircularButton(button: <#UIButton#>)
    }
}*/
